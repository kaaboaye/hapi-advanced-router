import { Controller as controller } from "./Controller";
import { Route as route } from "./Route";
import { ServerRoutes as serverRoutes } from "./Router";

export const Controller = controller;
export const Route = route;
export const ServerRoutes = serverRoutes;
